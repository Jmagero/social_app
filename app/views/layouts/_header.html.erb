<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <%= link_to 'Stay In Touch', root_path, class: 'navbar-brand' %>
    <% if user_signed_in? %>
    <div class="collapse navbar-collapse" id="navbarText">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <%= link_to 'Timeline', posts_path, class: 'nav-link' %>
            </li>
            <li class="nav-item">
                <%= link_to 'AllUsers', users_path, class: 'nav-link' %>
            </li>
            <li class="nav-item">
                <button type="button" class="btn btn-primary">
                    <%= link_to 'Sign_out', destroy_user_session_path, method: :delete, class: 'nav-link' %>
                </button>
            </li>
        </ul>
    <%else%>
        <li>
            <%= link_to 'Sign_up', new_user_registration_path, class: 'btn btn-secondary' %>
            <%= link_to 'Sign_in', user_session_path, class: 'btn btn-secondary' %>
        </li>    
    <% end %>
    </div>
</nav>
